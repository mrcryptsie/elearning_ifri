{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "uv venv --python 3.12 && uv pip install -r requirements.txt",
  "buildCommand": "uv run python manage.py makemigrations && uv run python manage.py migrate --noinput && uv run python manage.py collectstatic --noinput",
  "outputDirectory": ".",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
